<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
         version="3.0">

    <display-name>Recruitment Web Application</display-name>

    <!-- ============================================ -->
    <!-- FILTERS CONFIGURATION                        -->
    <!-- ============================================ -->
    
    <!-- Encoding Filter - Must be first to ensure proper character encoding -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>com.java_web.filter.EncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Logging Filter - Second to log all requests -->
    <filter>
        <filter-name>LoggingFilter</filter-name>
        <filter-class>com.java_web.filter.LoggingFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>LoggingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- Security Headers Filter - Add security headers (OPTIONAL - Uncomment for production) -->
    <!--
    <filter>
        <filter-name>SecurityHeadersFilter</filter-name>
        <filter-class>com.java_web.filter.SecurityHeadersFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>SecurityHeadersFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>
    -->

    <!-- CORS Filter - For API endpoints -->
    <filter>
        <filter-name>CORSFilter</filter-name>
        <filter-class>com.java_web.filter.CORSFilter</filter-class>
    </filter>
    
    <filter-mapping>
        <filter-name>CORSFilter</filter-name>
        <url-pattern>/api/*</url-pattern>
    </filter-mapping>

    <!-- Authentication Filter - Protect authenticated routes -->
    <filter>
        <filter-name>AuthenticationFilter</filter-name>
        <filter-class>com.java_web.filter.AuthenticationFilter</filter-class>
    </filter>
    
    <!-- Apply authentication filter to protected paths -->
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/profile/*</url-pattern>
    </filter-mapping>
    
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/dashboard/*</url-pattern>
    </filter-mapping>
    
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/apply/*</url-pattern>
    </filter-mapping>
    
    <filter-mapping>
        <filter-name>AuthenticationFilter</filter-name>
        <url-pattern>/saved-jobs/*</url-pattern>
    </filter-mapping>

    <!-- ============================================ -->
    <!-- STATIC RESOURCES                             -->
    <!-- ============================================ -->

    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/css/*</url-pattern>
    </servlet-mapping>
    
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/js/*</url-pattern>
    </servlet-mapping>
    
    <servlet-mapping>
        <servlet-name>default</servlet-name>
        <url-pattern>/images/*</url-pattern>
    </servlet-mapping>
</web-app>
